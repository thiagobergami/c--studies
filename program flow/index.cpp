#include <iostream>

using namespace std;

int main()
{
    /*
        for (int i{1}, j{5}; i<=5; i++, j++){
            cout << i << " * " << j << " : " << (i*j) << endl;
        }

        int scores[]{100, 90, 97};
        for (int score : scores)
        {
            cout << score << endl;
        }
        //tells to c++ compiler to figure out the declaration
        for (auto score : scores)
        {
            cout << score << endl;
        }

        for (auto c: "Frank"){
            cout << c << endl;
        }

   */
    /*     int i { 1 };
        while (i<=10)
        {
            if(i%2 == 0){
                cout << i << endl;
            }
            ++i;
        } */

/*     char selection{};

    do
    {
        double width{}, height{};
        cout << "Enter width and height ";
        cin >> width >> height;

        double area{width * height};

        cout << "The area is " << area << endl;
        cin >> selection;
    } while (selection == 'Y' || selection == 'y');

    cout << "Thanks!" << endl; */
    
}